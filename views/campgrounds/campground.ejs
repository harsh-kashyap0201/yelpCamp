<% layout('layouts/boilerplate.ejs') %> 
<script>
    document.title = "YelpCamp | Campgrounds🏕️";
</script>

<div class="container">
    <h1>All Campgrounds</h1>
        <% for (let camp of campgrounds) { %>
                <div class="card" style="width: 60vw;">
                    <div class="row">
                        <div class="col-md-4 m-auto p-4" >
                            <img src="<%= camp.image %>" alt="<%= camp.name %>" style="width: 100%;">
                        </div>
                        <div class="col-md-8 p-4">
                            <h2><%= camp.name %></h2>
                            <p><%= camp.location %></p>
                            <p><%= camp.description %></p>
                            <a href="/campgrounds/<%= camp.id %>"><button class="btn btm-sm btn-info ">More Info</button></a>
                        </div>
                    </div>
                </div>
                <br>
        <% } %>
    

    <a href="/campgrounds/new"" class="btn btn-sm btn-dark mb-3 ">Add new</a>
</div>
